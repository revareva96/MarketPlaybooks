version: '3.8'

networks:
  {{  SWARM_NETWORK_NAME }}:
    external: true

services:
  python:
    image: python:latest
    deploy:
      replicas: 1
    networks:
      - {{ SWARM_NETWORK_NAME }}
    environment:
      TEST: TEST
    command: 'tail -f /dev/null'